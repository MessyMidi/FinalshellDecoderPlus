# FinalshellDecoderPlus
FinalShell Batch Parsing Tool (with Windows GUI). Used to parse local FinalShell configuration files, extract server connection information, and decrypt stored passwords and private key data.
